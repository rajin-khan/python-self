#tuples are the same as lists, but immutable, so we cannot append, remove, or change elements.

x = (0, 2, 3, 4) #tuples have round brackets

x = [[], (), [[], []]] #you can have lists, or tuples inside a list, or a list in another list in another list, and so on.